{{- if .Values.udpRoute.enabled -}}
{{- $fullName := include "gateway-api.fullname" . -}}
{{- $udpRoute := .Values.udpRoute -}}
apiVersion: gateway.networking.k8s.io/v1alpha2
kind: UDPRoute
metadata:
  name: {{ $udpRoute.nameOverride | default (printf "%s-%s" $fullName ($udpRoute.name | default "")) | trunc 63 | trimSuffix "-" }}
  namespace: {{ include "gateway-api.namespace" . }}
  labels:
    {{- include "gateway-api.labels" . | nindent 4 }}
    {{- with $udpRoute.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with $udpRoute.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  parentRefs:
    {{- with $udpRoute.parentRefs }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with $udpRoute.rules }}
  rules:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
