{{- if .Values.httpRoute.enabled -}}
{{- $fullName := include "gateway-api.fullname" . -}}
{{- $httpRoute := .Values.httpRoute -}}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ $httpRoute.nameOverride | default (printf "%s-%s" $fullName ($httpRoute.name | default "")) | trunc 63 | trimSuffix "-" }}
  namespace: {{ include "gateway-api.namespace" . }}
  labels:
    {{- include "gateway-api.labels" . | nindent 4 }}
    {{- with $httpRoute.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with $httpRoute.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  parentRefs:
    {{- with $httpRoute.parentRefs }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with $httpRoute.hostnames }}
  hostnames:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with $httpRoute.rules }}
  rules:
    {{- tpl (. | toYaml) $ | nindent 4 }}
  {{- end }}
{{- end }}
