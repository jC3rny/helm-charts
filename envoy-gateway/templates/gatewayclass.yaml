{{ $values := index .Values "envoy-gateway" }}
apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: envoy
spec:
  controllerName: {{ $values.config.envoyGateway.gateway.controllerName }}
