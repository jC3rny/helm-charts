apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "test-app-110856.fullname" . }}-test-connection"
  labels:
    {{- include "test-app-110856.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "test-app-110856.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
