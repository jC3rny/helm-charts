apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "test-app-110206.fullname" . }}-test-connection"
  labels:
    {{- include "test-app-110206.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "test-app-110206.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
