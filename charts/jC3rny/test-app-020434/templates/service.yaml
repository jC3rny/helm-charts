apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-app-020434.fullname" . }}
  labels:
    {{- include "test-app-020434.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "test-app-020434.selectorLabels" . | nindent 4 }}
